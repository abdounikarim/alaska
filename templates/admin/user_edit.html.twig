{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
    <div class="content snow">
        <h1 class="text-center">Modifier un utilisateur</h1>
        <div class="row">
            <div id="user" class="col-md-10 offset-md-1">
                {{ include('admin/user_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                <br>
            </div>
        </div>
        <br>
        <p class="text-center"><a class="btn custom" href="{{ path('home') }}">Accueil</a></p>
    </div>

{% endblock %}